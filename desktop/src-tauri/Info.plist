<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>NSMicrophoneUsageDescription</key>
  <string>Request microphone access to transcribe audio locally</string>
  <!--
    MUTTER ships as an invisible background agent (a dictation daemon): no Dock
    icon, no global menu bar. LSUIElement makes it an "agent" app. When a window
    IS shown (settings, via relaunching the app), show_main_window() flips the
    activation policy to Regular so a Dock icon appears for that session, then
    back to accessory on close. Tauri auto-merges this into the bundle Info.plist.
  -->
  <key>LSUIElement</key>
  <true/>
</dict>
</plist>