# Decisions

Cross-platform only. Per-platform: `mac/DECISIONS.md`, `android/DECISIONS.md`. Newest at top.

- **2026-07-06 — Mac substrate = vendored Handy, not the hand-rolled Python daemon (Peter).** The Python daemon's fragility was 100% its audio/event-tap layer (PortAudio teardown deadlock → process-suicide loop; CGEventTap dropping fn-up). Rather than keep patching it, vendor [Handy](https://github.com/cjpais/Handy) (MIT, native Rust/Tauri) into `desktop/` via `git subtree --squash` and keep our shared MLX whisper service as the ASR brain via a new `RemoteSocket` engine. **Vendor, not fork** — one repo, adopt upstream by release tag only, custom logic in new files + 1-line seams (`desktop/PATCHES.md`). Auto-updater disabled (it would overwrite our tree). The two root failures are structurally gone (cpal `Stream::Drop`, tap re-enable+reconcile) or healed in-process. Python daemon retained until soak + R1–R18 parity gate clears.
- **2026-06-10 — Split into mac/ + android/ + ios/ subprojects (Peter).** Each platform self-contained with own DECISIONS.md; root holds umbrella README, brand `docs/`, this file. CLAUDE.md trashed — pure gitnexus block, byte-dup of AGENTS.md, taught agents nothing while auto-loading every session.
