# Decisions — mac-native

Architectural only. Newest at top.

- **2026-08-05 — A forwarded fn press must never reach the microphone (`hardware.rs`).** Screen Sharing forwards the remote user's fn as a real `keycode=63` through the same `kCGSessionEventTap` this daemon and the Python one both use — measured `src_pid` = `ScreensharingAgent`, hardware posts `src_pid 0`. It is indistinguishable by keycode, so `daemon.py`'s `keycode != _KEYCODE_FN` filter never blocked it either: **both** daemons recorded on every remote dictation, and only R4 kept the Python one silent. `CGEventSourceFlagsState(kCGEventSourceStateHIDSystemState)` is ground truth for the physical keyboard and showed zero transitions across a 3.2 s forwarded hold (`daemon.py:534` already trusted it to heal a missed fn-up). Sampled across the whole hold rather than once at key-down, so a lagging read cannot misclassify a real local press; recording starts immediately, so a real dictation pays no latency. Consequence: a Mac being driven remotely stops dictating from its own mic — correct, nobody is at that mic, and the operator's own machine keeps typing into the session via `keycodes.rs`. Tradeoff: dictation from a non-physical keyboard (remote KVM, some accessibility tools) is unreachable.

- **2026-08-05 — R4 restored, with the threshold relative to the clip's own floor (`speech.rs`).** The `mac-native` rewrite kept the fn trigger and dropped `stt.py`'s acoustic gate, leaving only the 0.3 s length check — which is why a silent hold types boilerplate. Structure and constants are `stt.py`'s, unchanged. Threshold is `max(300, clip_floor × 2)` — never looser than `DEFAULT_SILENCE_RMS`, so a quiet clip resolves to exactly 300 and behaves as the Python daemon did. Verbatim 300 is *inert* on code-mac: empty-room floor reads int16 RMS ~1030 at 100 % input gain, passing 12/12 silent clips as speech. High-passed at 250 Hz first because this room's floor is low-frequency (fan / mic self-noise); that is what separates ambience (3.3–4.6 dB dynamic range) from quiet speech (8.7–45 dB). Lowering input gain so verbatim 300 works was tested and rejected — viable only at ≤25 % gain, total margin 4.1 dB, and it fails *silently* when the fan spins up.

- **2026-07-26 — Capture from the built-in mic always, never the system-default input (R3).** Ported from the Python daemon (`stt._builtin_input_device`, 2026-06-09) and the vendored Handy config; the `mac-native` rewrite had silently regressed to `default_input_device()`. Two defaults capture silence: a Bluetooth headset (opening it forces A2DP→HFP, which fails `-10851` while audio plays and otherwise yields rms=0) and a headphone-jack "External Microphone" on headphones with no mic. Both present as a *successful* capture, so the silence reaches whisper, comes back as boilerplate, and `hallucination.rs` correctly drops it — nothing types and nothing errors. Selection is by device name (`is_builtin_mic`), falling back to the default only on a Mac with no built-in mic. Tradeoff: external/BT mics are unreachable; gate on a user setting if that is ever wanted.

- **2026-07-08 — A silent fn-hold must type nothing: transcript guards are part of the pipeline contract.** Whisper emits boilerplate on silent/near-silent audio ("You", "Thank you."). Two guards sit between capture and injection, ported from `stt.py`: clips under 0.3 s never reach whisper (`MIN_CLIP_SAMPLES`), and every transcript must pass `hallucination.rs`. Removing either makes every accidental/empty fn-hold type " You" at the cursor — including into Screen Sharing fields, where an unfiltered daemon on the *other* machine (triggered by the forwarded fn) interleaves " You" mid-word into otherwise-perfect dictation. Every machine running mutter needs a build with these guards.

- **2026-07-08 — Screen Sharing detection asks LaunchServices (`lsappinfo`), not the AX API.** The Python daemon's system-wide `AXFocusedApplication` query fails unconditionally on Darwin 25 from this daemon's process shape — kAXErrorCannotComplete (-25204) on every call, AX-trusted or not, with or without a pumped CFRunLoop (probed standalone, both variants). NSWorkspace's `frontmostApplication` cache never refreshes without a pumped main run loop (main thread blocks in the hotkey loop). CGWindowList z-order was probed live and diverged from actual app activation. `lsappinfo front` tracks activation — the thing that decides where keystrokes land — in real time; one subprocess per dictation, same idiom as the osascript mute toggle.

- **2026-07-06 — Minimal native daemon, not the vendored Handy/Tauri app (Peter).** Supersedes the root 2026-07-06 "Mac substrate = vendored Handy" decision. Handy's robustness lived entirely in two low-level choices, not in Rust or its GUI. So instead of carrying a 36 MB Tauri app + webview + overlay + settings UI to run invisibly, extract just those two choices into a ~1.9 MB headless daemon that behaves exactly like the old Python daemon. The GUI added latency (transcription overhead is unchanged — the ~1.2 s MLX floor is shared) and a recording overlay the user never wanted. Vendored `desktop/` superseded (retire after soak).

- **The two robustness choices copied (grounded in code, not vibes).**
  1. *fn tap:* `handy-keys` re-enables the CGEventTap every 100 ms and re-derives fn from OS flags every event. The Python daemon armed the tap once + blocked in `CFRunLoopRun`; nothing re-armed a tap macOS disabled → dropped fn-up.
  2. *audio teardown:* cpal `Stream::drop` is a clean stop→dispose that never re-enters the CoreAudio HAL lock; teardown is timeout-bounded regardless. PortAudio `Pa_CloseStream` re-entered the HAL lock → deadlock after sleep/wake → Python's only cure was `os._exit(1)` + respawn (unfixable in Python: leaking the wedged close-thread poisons the held HAL lock).

- **Reuse the `com.peter.mutter.app` bundle identity for TCC.** Accessibility can't be granted programmatically and TCC keys on bundle identity, not the binary. A minimal `LSUIElement` bundle + stable ad-hoc identifier lets Accessibility + Microphone grants survive every rebuild — no re-prompting on update. *Correction 2026-07-08 (proven live):* for ad-hoc-signed apps TCC additionally pins the Accessibility grant to the binary cdhash — Microphone survives rebuilds, Accessibility does not. Every build from changed source costs one off/on toggle; a bit-identical rebuild of the granted commit re-arms with zero asks. After several different binaries cycle under the identity the row can wedge — recover with `tccutil reset Accessibility com.peter.mutter.app`, kickstart, toggle fresh.

- **Capture at device-native rate, resample to 16 kHz in software (`resample.rs`).** Deliberately the opposite of the Python daemon forcing the hardware clock to 16 kHz, which was its Bluetooth-codec-negotiation fragility. The whisper server assumes 16 kHz (`len/16000`), so the daemon owns the conversion.

- **ASR stays out-of-process (shared MLX whisper service).** The daemon decodes + resamples audio and hands 16 kHz mono PCM over a unix socket. One always-warm model shared by every consumer; the daemon never loads weights. Wire protocol is byte-for-byte the Python reference's.
